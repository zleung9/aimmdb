trees:
  - path: /
    tree: aimmdb:AIMMTree.from_uri
    args:
      uri: ${MONGO_URI}
      username: root
      password: ${MONGO_PASSWORD}
      collection_name: "tiled"
authentication:
  allow_anonymous_access: false
  single_user_api_key: abc
media_types:
  node:
    application/x-hdf5: aimmdb:serialize_hdf5
